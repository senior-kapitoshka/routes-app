{
  "name": "routes-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && ng build --configuration=production",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "18.x"
  }
}
